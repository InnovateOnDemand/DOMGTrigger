name: Deprecated - legacy workflow (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "This workflow is disabled. Use master_uploadtriggerv2.yml instead."
